<h1>golf club</h1>
<h3>Tweet詳細</h3>
<div class="tweet">
  <p><%= @tweet.club %></p>
  <p><%= @tweet.created_at %></p>
</div>
<% @tweet.tags.each do |tag| %>
    <span><%= tag.name %></span>
<% end %>
<%= link_to "Tweet一覧に戻る", tweets_path %>